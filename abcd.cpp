#include<bits/stdc++.h>
using namesapce std;
int main()
{
    int n,k,i;
    cin>>n>>k;
    vector<int>qua(n),qua_past(n);
    for(i=0;i<n;i++)//quantity needed for baking
        cin>>qua[i];
    for(i=0;i,n;i++)//quantity already at hand
        cin>>qua_past[i];
    int low=0,high=1005,mid;
    while(low<=high)
    {
        mid=l+(h-l)/2;//number of cookies to be baked
        
    }
}